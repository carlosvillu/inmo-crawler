fs  = require 'fs'

{print} = require 'util'
{spawn} = require 'child_process'

task 'open', 'Open the browser point to server', ->
  spawn 'open', ['www.google.com']
  print 'Opening the browser...\n'
